---
'@kernhq/module-tracker': patch
---

The import page's file picker is a proper button with the chosen file's name beside it, instead of
the browser's own unstyled "Choose File — No file chosen" control.
